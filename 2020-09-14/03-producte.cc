// Programa que llegeix tres nombres i escriu el seu producte

#include <iostream>
using namespace std;

int main()
{
    // llegim les dades
    int a, b, c;
    cin >> a >> b >> c;
    // calculem el resultat
    int p = a * b * c;
    // escrivim el resultat
    cout << p << endl;
}
