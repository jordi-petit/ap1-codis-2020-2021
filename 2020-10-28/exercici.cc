// exercici

#include <vector>
using namespace std;

int minim(const vector<double>& v)
{
    ? ? ?
    // n comparacions on n = v.size()
}

int maxim(const vector<double>& v)
{
    ? ? ?
    // n comparacions on n = v.size()
}

void minim_maxim(const vector<double>& v, double& min, double& max)
{
    min = minim(v);
    max = maxim(v);
    // 2n comparacions on n = v.size()
}

void minim_maxim(const vector<double>& v, double& min, double& max)
{
    ? ? ?
    // menys de 2n comparacions on n = v.size()
    // 1.5n comparacions
}
